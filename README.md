# Lumen Test with Simple Auth

# Install
- cp ```.env.example``` to ```.env```
- Setup ```.env``` and setup ```Database``` (Environtment)
- Run ```composer install```

# Run
- Run ```php -S localhost:8000 -t public```
- Happy Coding!
